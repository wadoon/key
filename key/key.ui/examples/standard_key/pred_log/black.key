// Formale Systeme Klausur WS 2014/2015
// Aufgabe 4c.

\sorts { K; }
\predicates { s(K); }
\problem {
  // Musterlösung
  \forall K x; \forall K y; \forall K z;
  	( s(x) & s(y) & s(z) -> x=z | y=z )
->
  // eine fast richtige Abgabe
  \exists K x; \exists K y; \forall K z;
	( s(z) -> z = x | z = y )

}
