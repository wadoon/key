example.name = Remove Control Flag
example.path = Abstract Execution/
example.file = RemoveControlFlag.aer

A model of Martin Fowler's "Remove Control Flag" refactoring. Based on the idea of abstract strongest loop invariants. The whole invariant has to be established before the break introduced by the refactoring, otherwise, it does not work.
