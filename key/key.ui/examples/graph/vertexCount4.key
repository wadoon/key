/*
 not provable, we need first to define a function that computes the vertices common to both subgraphs
 */
\problem {

 vertexCount(connect(edgeLabel(1),
	           connect(edgeLabel(2), vertex(vertexLabel(1)), vertex(vertexLabel(2))),
                   vertex(vertexLabel(3)))) = 3

}