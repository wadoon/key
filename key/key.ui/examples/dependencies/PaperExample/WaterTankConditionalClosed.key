/*
Following both if and else path we end up in WaR but not RaW.
Closed.
*/
\javaSource "src/";

\predicates{
  
}

\programVariables {
  WaterTank wt1;
}

\problem {

	wt1 != null & noW(singleton(wt1, WaterTank::$currentAmount)) & noRaW(singleton(wt1, WaterTank::$currentAmount)) -> 
	\<{ if(wt1.currentAmount > 0)
			wt1.currentAmount = wt1.capacity - wt1.currentCapacity;
		else
			wt1.currentAmount = 0;}\>noRaW(singleton(wt1, WaterTank::$currentAmount)) 
}