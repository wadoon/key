\javaSource "src/";

\predicates{
  
}

\programVariables {
  int[] array;
  int i;
}


\problem {
 /*
 	
     i>=0 & i<=array.length-1 & noW(arrayRange(array,i,array.length-1)) &  noR(arrayRange(array,i+1,array.length-1)) & (i > 0 -> noRaW(arrayRange(array,0,i-1)))
     
     arrayRange(array, 0,array.length-1)
     array.length - i
  */
  array!=null & array.length >= 1 & wellFormed(heap) & 
  noR(arrayRange(array,0,array.length-1)) & noW(arrayRange(array,0,array.length-1))
   -> {i:=0} \<{ 
                  while(i<array.length-1){
   					array[i]=array[i+1];
   					i++;
   				} 
   			}\>noRaW(arrayRange(array,0,array.length-1)) 
}