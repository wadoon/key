\javaSource "src/";

\predicates{
  
}

\programVariables {
  int[] array;
  int i;
}


\problem {
 /*
 	 loop invariant by us:
     i>=0 & i<=array.length & noR(allFields(array))
     
     allFields(array)
     array.length - i
  */
  array!=null & wellFormed(heap) & noR(allFields(array)) & noW(allFields(array))
   -> {i:=0} \<{ 
                  while(i<array.length){
   					array[i]=1;
   					i++;
   				} 
   			}\>noRaW(allFields(array)) 
}