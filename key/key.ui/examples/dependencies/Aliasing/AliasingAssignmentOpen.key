/*
Here we have aliasing so o and p are same objects. So there is a RaW.
It supposed to remain open.
Current State: Not Compiled.
*/

\javaSource "src/";

\predicates{
  
}

\programVariables {
  A o;
  A p := o;
  int v;
}

\problem {

  o != null & p != null & o=p & noW(singleton(o, A::$a)) & noRaW(singleton(o, A::$a)) -> \<{ o.a = p.a ; v = p.a; }\>noRaW(singleton(o, A::$a)) 
}