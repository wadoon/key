\sorts {
  Graph;
  VertexLabel;
  EdgeLabel;
}

\functions {

   // constructors
   Graph emptyGraph;
   Graph vertex(VertexLabel);
   Graph connect(EdgeLabel, Graph, Graph);

  // current vertex/edge labels
   \unique VertexLabel vertexLabel(int);
   \unique EdgeLabel zero;
   \unique EdgeLabel edgeLabel(int);

   //auxiliary constructors
   Graph edge(EdgeLabel,VertexLabel,VertexLabel);
   Graph vertices(Seq);

   // functions on graphs
   int vertexCount(Graph);
   int edgeCount(Graph);

}

\predicates {

  isEmptyGraph(Graph);
  hasVertex(VertexLabel, Graph);
  hasEdge(VertexLabel, VertexLabel, Graph);
}
