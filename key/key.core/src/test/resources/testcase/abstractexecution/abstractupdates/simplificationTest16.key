\settings {"[StrategyProperty]OSS_OPTIONS_KEY=OSS_OFF"}

\functions {
  LocSet frame;
  LocSet footprint;
  Heap h;
  Object o;
  Field f;
}

\problem {
      !elementOf(o,f,frame)
    ->
      any::select({U_P(frame:=footprint)}heap,o,f) = any::select(heap,o,f) 
}
