\settings {"[StrategyProperty]OSS_OPTIONS_KEY=OSS_OFF"}

\functions {
    LocSet locs;
    Heap t;
    Heap h;
}

\problem {
    {heap := t || heap := h} value(locs) =
    {heap := h || heap := t} value(locs)
}