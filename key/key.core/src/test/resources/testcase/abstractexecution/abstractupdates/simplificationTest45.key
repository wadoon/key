\settings {"[StrategyProperty]OSS_OPTIONS_KEY=OSS_OFF"}

\functions {
    LocSet locs;
    int t;
    Heap h;
}

\programVariables {
    int v;
}

\problem {
    {v := t || heap := h} value(locs) =
    {heap := h || v := t} value(locs)
}
