\functions {
  LocSet frame1;
  LocSet frame2;
  LocSet footprint;
  Heap h;
  Object o;
  Field f;
}

\programVariables{
  int x;
}

\problem {
      !elementOf(o,f,frame1) &
      !elementOf(o,f,frame2)
    ->
      any::select({U_P(x,frame1,frame2:=footprint)}heap,o,f) = any::select(heap,o,f) 
}
