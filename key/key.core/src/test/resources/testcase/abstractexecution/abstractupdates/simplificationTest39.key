\functions {
  LocSet subFrame(int);
}

\programVariables {
  int w;
  int x;
  int y;
  int z;
}

\problem {
    {x := 42 || y := mul(z,2) || w := 19} value(subFrame(jdiv(x, mul(y, z))))
  = {w := 19} value(subFrame(jdiv(42, mul(mul(z,2), z))))
}
