\sorts {
    kGame;
}

\functions {    
    int getSign1(kGame);
    
    int getSign2(kGame);

    int getWinner(kGame);

    kGame newGame(int, int, int);

    kGame object2game(Heap, Object);

    Seq serializeGame(kGame);

    kGame deserializeGame(Seq);

    kGame readGame(Seq,int);
}

