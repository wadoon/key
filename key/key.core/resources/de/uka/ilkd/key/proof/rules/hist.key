\sorts {
	Event;
	CallDirection;
	EventType;
	Method;
}

\functions {
	//constructors
	//direction, type, partner, method, arguments / return value, current heap
	Event event(CallDirection, EventType, Object, Method, Seq, Heap);
	CallDirection incoming;
	CallDirection outgoing;
	EventType methodCall;
	EventType methodTermination;

	//destructors
	CallDirection getDirectionFormEvent(Event);
	EventType getTypeFormEvent(Event);
	Object getPartnerFormEvent(Event);
	Method getMethodFormEvent(Event);
	Seq getArgumentsFormEvent(Event);
	Heap getHeapFormEvent(Event);
}

\programVariables {
    Seq hist;
}