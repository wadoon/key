\sorts {
	Event;
	EventType;
	Method;
}

\functions {
	//constructors
	//type, caller, callee, method, arguments / return value, current heap
	Event event(EventType, Object, Object, Method, Seq, Heap);
	EventType methodCall;
	EventType methodTermination;

	//destructors
	EventType getTypeFromEvent(Event);
	Object getCallerFromEvent(Event);
	Object getCalleeFromEvent(Event);
	Method getMethodFromEvent(Event);
	Seq getArgumentsFromEvent(Event);
	Heap getHeapFromEvent(Event);
}

\predicates {
    wfHist(Seq);
}

\programVariables {
    Seq hist;
}