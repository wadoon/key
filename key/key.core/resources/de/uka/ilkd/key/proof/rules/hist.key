\sorts {
	Event;
	CallDirection;
	EventType;
	Method;
}

\functions {
	//constructors
	//direction, type, partner, method, arguments / return value, current heap
	Event event(CallDirection, EventType, Object, Method, Seq, Heap);
	CallDirection incoming;
	CallDirection outgoing;
	EventType methodCall;
	EventType methodTermination;

	//destructors
	CallDirection getDirectionFromEvent(Event);
	EventType getTypeFromEvent(Event);
	Object getPartnerFromEvent(Event);
	Method getMethodFromEvent(Event);
	Seq getArgumentsFromEvent(Event);
	Heap getHeapFromEvent(Event);
}

\predicates {
    wfHist(Seq);
}

\programVariables {
    Seq hist;
}