/*
 *
 */
module VariableModule;
export *;

interface Variable {
    Unit send(Int i);
}

class VariableImpl implements Variable {
    Int value;

    Int send(Int i) {
    	this.value = i;
    	return i;
    }

    Unit test(List<Int> l) {}
}

{ }
