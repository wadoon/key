/*
 *
 */
module VariableModule;
export *;

interface Variable {
    Unit send(Int i);
}

class VariableImpl implements Variable {
    Int value;

    Int send(Int i) {
    	this.value = i;
    	return i;
    }

    Unit test() {
    	 Fut<Int> r = this!send(1);
 	 Int i = r.get();
	 this.value = 1; 
	 this.value = this.value + 1;
    }
}

{ }
