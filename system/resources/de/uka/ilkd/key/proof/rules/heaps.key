// Sequences of heaps, used in rely/guarantee

\functions {
   //select/store
    alpha alpha::_select(Seq, Object, Field);
    Seq _store(Seq, Object, Field, any);
    Seq _create(Seq, Object);
    Seq _anon(Seq, LocSet, Heap);
    Seq _memset(Seq, LocSet, any);
}

\programVariables {
    Seq heaps;
}
